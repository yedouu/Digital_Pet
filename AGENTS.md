# AGENTS.md

本文件是本项目给 Codex 的项目级长期说明。开始任何任务前，请先读取并遵守这里的规则。

## 工作方式

- 只读取完成当前任务必须的文件。
- 不要为了“了解项目”扫描整个仓库。
- 修改前先说明本次需要查看或修改哪些文件。
- 保持最小改动，不要顺手重构。
- 不要改动与当前需求无关的文件。
- 不要自动格式化全项目。

## 修改与验证

- 修改代码后，优先运行与改动相关的最小验证。
- 如果改动影响构建，运行：

```powershell
npm.cmd run build
```

- 如果只是 README 或文档改动，不需要运行完整构建。

## Git 规则

每次修改完成后都要提交并推送到当前分支：

```powershell
git add .
git commit -m "简短说明本次修改"
git push
```

提交信息要简短、具体，说明本次实际改了什么。

## 项目约定

- 默认分支工作在 `codex/desktop-pet-mvp`。
- README 使用中文说明。
- DeepSeek API Key 不提交到仓库，使用 `.env.local` 本地配置。
- `.env.example` 只保留示例字段，不放真实密钥。
- 普通用户更新方式以 GitHub Release 安装包为主。

## 回复风格

- 用中文回复。
- 直接说明做了什么、改了哪些文件、是否验证、是否提交推送。
- 如果因为权限或网络无法提交/推送，请给出用户可直接复制执行的命令。
